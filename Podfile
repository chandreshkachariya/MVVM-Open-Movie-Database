# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Open Movie Database' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Open Movie Database
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'SDWebImage'
  
  target 'Open Movie DatabaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Open Movie DatabaseUITests' do
    # Pods for testing
  end

end
